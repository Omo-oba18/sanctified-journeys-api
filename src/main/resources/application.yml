#spring:
#  datasource:
#    url: jdbc:postgresql://localhost:5432/sanctified_journeys
#    username: chablis
#    password: JesuS_0nly.Yu1
#    driver-class-name: org.postgresql.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#      database: postgresql
#      database-platform: org.hibernate.dialect.PostgreSQLDialect
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sanctified_journeys?useSSL=false&serverTimezone=UTC
    username: root
    password: JesuS_0nly.Yu1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
        database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect